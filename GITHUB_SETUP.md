# Инструкция по загрузке проекта QuizByte на GitHub

## Шаг 1: Установка Git (если еще не установлен)

1. Скачай Git с официального сайта: https://git-scm.com/download/win
2. Установи Git, следуя инструкциям установщика
3. Перезапусти терминал/Android Studio после установки

## Шаг 2: Инициализация Git репозитория

Открой терминал в папке проекта (`C:\projects`) и выполни:

```bash
git init
git add .
git commit -m "Initial commit: QuizByte - gamified IT interview prep app"
```

## Шаг 3: Создание репозитория на GitHub

1. Зайди на https://github.com и войди в свой аккаунт (или создай новый)
2. Нажми кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполни:
   - **Repository name**: `quizbyte` (или любое другое имя)
   - **Description**: "Gamified IT interview preparation app with Python quizzes"
   - Выбери **Public** или **Private**
   - **НЕ** ставь галочки на "Initialize with README", "Add .gitignore", "Choose a license" (у нас уже есть файлы)
4. Нажми **"Create repository"**

## Шаг 4: Подключение к GitHub и загрузка кода

После создания репозитория GitHub покажет инструкции. Выполни в терминале:

```bash
git remote add origin https://github.com/ТВОЙ_НИК/quizbyte.git
git branch -M main
git push -u origin main
```

**Важно:** Замени `ТВОЙ_НИК` на свой GitHub username!

Если GitHub попросит авторизацию:
- Используй **Personal Access Token** (не пароль)
- Создай токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выбери права: `repo` (полный доступ к репозиториям)
- Скопируй токен и используй его как пароль при `git push`

## Готово! 🎉

Твой код теперь на GitHub. Можешь открыть репозиторий в браузере и увидеть все файлы проекта.

